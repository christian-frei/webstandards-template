# webstandards-template
## Usage
```shell
git clone https://github.com/christian-frei/webstandards-template.git myProject
```

run with browser-sync
```shell
alias bsync='browser-sync src -f src --cors -b "google chrome" --no-notify'
```